# Supón que x y y son variables de tipo float
x = float(input("Número X: "))
y = float(input("Número Y: "))

# Calcula z según la fórmula dada
z = (1 + (x**2 / y)) / ((x**3) / (1 + y))

print(f"Este es el resultat: {z}")
